# ABFI Platform - pnpm configuration
# See: https://pnpm.io/npmrc

# Allow specific packages to run their postinstall/install scripts
# These are required for proper functionality:
#
# @tailwindcss/oxide - Native Rust-based Tailwind CSS compiler
#                      Provides 10x faster builds via native bindings
#
# core-js - JavaScript polyfills for browser compatibility
#           Runs postinstall to compile native extensions
#
# esbuild - Fast JavaScript/TypeScript bundler
#           Installs platform-specific native binary for fast builds
#
# protobufjs - Protocol Buffers for JavaScript
#              Used by gRPC dependencies (Supabase, etc.)
#              Compiles native bindings for performance

# Enable build scripts for these specific dependencies
enable-pre-post-scripts=true
